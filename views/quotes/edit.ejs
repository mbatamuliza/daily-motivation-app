<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Quote | Daily Motivation</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <%- include('../partials/_navbar.ejs')%>

    <main class="auth-container">
        <h1>Edit Your quote</h1>

        <form action="/quotes/<%= quote._id %>?_method=PUT" method="POST">
            <div clas="form-group">
                <label for="content">Write Your Quote:</label>
                <textarea name="content" id="content" required><%= quote.content %></textarea>
            </div>

            <div class="form-group">
                <label for="author">Author (Optional):</label>
                <input type="text" name="author" id="author" value="<% quote.author || '' %>" placeholder="Anonymous">
            </div>

            <div class="form-actions">
                <button type="submit" class="btn">Update Quote</button>
                <a href="/quotes" class="btn-cancel">Cancel</a>
            </div>
        </form>
    </main>
</body>
</html>